import { DataSortPipePipe } from './data-sort-pipe.pipe';

describe('DataSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
